#include<stdio.h>
int main()
{
    int n, i;
    printf("Enter any number = ");
    scanf("%d",&n);
    printf("First %d even natural numbers in reverse order are = ", n);
    for(i=2*n;i>=2;i=i-2)
    {
        printf("%d ", i);
    }
    return 0;
}
