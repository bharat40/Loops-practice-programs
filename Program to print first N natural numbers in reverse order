#include<stdio.h>  // n natural numbers in reverse order
int main()
{
    int n, i;
    printf("Enter any number = ");
    scanf("%d",&n);
    printf("first %d natural numbers in reverse order are = ", n);
    for(i=n;i>=1;i--)
    {
        printf("%d ",i);
    }
    return 0;
}
