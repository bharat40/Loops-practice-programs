#include<stdio.h> // cube of first n natural numbers
int main()
{
    int n, i;
    printf("Enter any number = ");
    scanf("%d",&n);
    printf("cube of first %d natural numbers are = ", n);
    for(i=1;i<=n;i++)
    {
        printf("%d ", i*i*i);
    }
    return 0;
}
